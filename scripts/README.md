# OceanSense model export

Run this before starting the API:

```powershell
python scripts/export_models.py
```

The script trains from the source CSV files in the parent workspace and writes deployable artifacts into `models/`.
